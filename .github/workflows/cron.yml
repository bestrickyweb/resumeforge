name: Hourly Cron Job
on:
  schedule:
    - cron: '0 * * * *' # This runs every hour for free on GitHub!
jobs:
  trigger-vercel-api:
    runs-on: ubuntu-latest
    steps:
      - name: Call API Route
        run: curl -X GET "https://your-vercel-project-url.vercel.app/api/my-task"